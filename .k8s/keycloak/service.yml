apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: fixit
spec:
  selector:
    app: keycloak
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30085
  type: NodePort